# from unittest import mock
#
# import pytest
#
# from sarafan.client import SarafanClient
#
# from .utils import generate_rnd_hash
#
#
# @pytest.mark.asyncio
# async def test_client_simple():
#     p = Peer(service_id='xyz')
#     client = SarafanClient(bootstrap_peers=[p])
#     with mock.patch.object(p)
#     path = await client.download(generate_rnd_hash()[2:])
